name: print text from demo.html
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
    - uses: actions/checkout@v2
    
    - name: print
      run: cat demo.html
